// Design submission + approve / request revision — to be implemented
export const submissionService = {
  listByCard: async (cardId: string) => [],
  create: async (cardId: string, data: object) => null,
  approve: async (submissionId: string) => null,
  requestRevision: async (submissionId: string, comment: string) => null,
};
