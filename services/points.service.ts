// Designer points ledger + monthly summary — to be implemented
export const pointsService = {
  addForApproval: async (designerId: string, points: number, submissionId: string) => null,
  getSummaryByUser: async (userId: string, month?: string) => ({ total: 0, history: [] }),
};
