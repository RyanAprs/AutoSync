"use server";
// Server actions for card forms — to be implemented
export async function createCard(columnId: string, formData: FormData) {
  return { success: false as const, error: "Not implemented" };
}
